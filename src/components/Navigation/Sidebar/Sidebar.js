import React from 'react';
import SideDrawer from './SideDrawer'

const sidebar = (props) => {
    return (
        <React.Fragment>
            <SideDrawer />
        </React.Fragment>
    );
};

export default sidebar;